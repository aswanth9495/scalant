import Button from './Button';

// meta data
export default {
  title: 'Button',
  component: Button,
};

// variations
export const Default = () => <Button>Click Me</Button>;

Default.args = {
  // args for this variation
};
